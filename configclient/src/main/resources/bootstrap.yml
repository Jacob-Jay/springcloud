
spring:
  application:
    name: configClient
  cloud:
    config:
      name: s
      profile: dev
#      uri: http://localhost:8994/
      discovery:
        enabled: true
        service-id: configServer

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/